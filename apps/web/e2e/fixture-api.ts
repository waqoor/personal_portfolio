throw new Error(
  "The parallel fixture API has been retired. Start the canonical Docker Compose stack, "
  + "seed tests/fixtures/e2e/manifest.json, and run Playwright against PORTFOLIO_E2E_BASE_URL.",
);
